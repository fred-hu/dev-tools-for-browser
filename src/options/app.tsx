import React, { useState } from 'react';

function IndexOptions(): React.ReactElement {
  return (
    <div
      style={{
        padding: 16,
        minWidth: 400,
      }}>
      <h2>dev tools</h2>
    </div>
  );
}

export default IndexOptions;
